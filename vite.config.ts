import { defineConfig } from 'vite'

// Minimal Vite config — React JSX will be handled by esbuild.
// @note: If you prefer React Fast Refresh, install a matching @vitejs/plugin-react
// version compatible with your Vite release and re-enable the plugin here.
export default defineConfig({})
